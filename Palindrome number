class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """

        x=str(x)
        x1=x[::-1]
        if(x1==x):
            return True
        else:
            return False
